@model IEnumerable<LectorASPAutoGenerated.ProjectTSDotNETServiceReference.Subject>
<h1> GetSubjects </h1>
<ul>
    @foreach (var item in Model)
    {
        <li>
            @string.Format("Id = {0}; Title = {1}",
            item.SubjectId, item.Title)
        </li>
    }
</ul>

<h1><span hiddenField="id invizibil">DataBase</span></h1>

